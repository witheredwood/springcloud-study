spring:
  profiles: dev
  
---
serve:
  port: 8201
# spring 配置
spring:
  profiles: dev
  application:
    name: springcloud-config-clint

# Eureka 配置
eureka:
  client:
    service-url:
      defaultZone: http://localhost:7001/eureka/

---
serve:
  port: 8202
# spring 配置
spring:
  profiles: test
  application:
    name: springcloud-config-clint

# Eureka 配置
eureka:
  client:
    service-url:
      defaultZone: http://localhost:7001/eureka/
